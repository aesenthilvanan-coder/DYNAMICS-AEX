Optional bundled or custom GROMACS forcefield assets.

The default Docker image uses system GROMACS force fields (e.g. amber99sb-ildn). Place extra forcefield or water model files here only if you override MDP or topology generation to reference local paths.
